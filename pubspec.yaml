name: kaapav_app
description: KAAPAV — Most Advanced WhatsApp Business Suite with E-Commerce
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  jwt_decoder: ^2.0.1
  crypto: ^3.0.3
  http_parser: ^4.0.2
  permission_handler: ^11.3.0

  # State Management
  flutter_riverpod: ^2.5.1

  # Networking
  dio: ^5.4.1
  web_socket_channel: ^3.0.3

  # Local Database (Offline-first)
  drift: ^2.15.0
  sqlite3_flutter_libs: ^0.5.20
  path_provider: ^2.1.2
  path: ^1.9.0

  # Firebase (Push Notifications)
  firebase_core: ^3.8.1
  firebase_messaging: ^15.1.5
  firebase_analytics: ^11.3.5
  firebase_crashlytics: ^4.1.5
  flutter_local_notifications: ^18.0.1

  # Auth & Security
  flutter_secure_storage: ^9.0.0
  local_auth: ^2.3.0

  # UI Components
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  flutter_slidable: ^3.0.1
  photo_view: ^0.15.0
  intl: ^0.19.0
  timeago: ^3.6.1

  # Media
  image_picker: ^1.0.7
  file_picker: ^8.0.0
  audioplayers: ^6.0.0
  #record: ^5.0.5

  # Utils
  connectivity_plus: ^5.0.2
  url_launcher: ^6.2.4
  share_plus: ^7.2.2
  flutter_dotenv: ^5.1.0
  uuid: ^4.3.3
  json_annotation: ^4.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.8
  drift_dev: ^2.15.0
  json_serializable: ^6.7.1
  flutter_sound: ^9.2.13

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/
    - assets/icons/
    - assets/sounds/